using Application.Authentication.Queries.Authenticate;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace WebApi.Controllers.Profile
{
    //public class ProfileController : ApiControllerBase
    //{
    //    [Authorize(Policy = "UserIdPolicy")]
    //    [HttpGet("{id}")]
    //    public async Task<IActionResult> Get(AuthenticateCommand authData)
    //    {
    //        var profileInfo = Mediator.Send();
    //        var token = await Mediator.Send(authData);

    //        return Ok(new { token });
    //    }
    //}
}
